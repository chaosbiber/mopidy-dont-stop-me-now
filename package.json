{
  "name": "mopidy-dont-stop-me-now",
  "version": "1.0.0",
  "description": "Don't stop (me) now: keep Mopidy playing by adding random tracks if the tracklist runs dry. Works best with consume mode.",
  "main": "index.js",
  "scripts": {
    "play4ever": "node index.js",
    "postinstall": "test ! -f .env && cp .env.example .env || exit 0"
  },
  "author": "Marcel Bieberbach",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "lodash": "^4.17.21",
    "mopidy": "^1.3.0"
  }
}
